{
  "previewPort": 3000,
  "healthPath": "/health",
  "startCommand": "docker compose -f docker-compose.base44.yml up -d",
  "secrets": [
    {
      "name": "GEMINI_API_KEY",
      "description": "Google Gemini API key for the AI audit and team-assignment features. Optional — the app falls back to rule-based heuristics without it. Obtain from https://aistudio.google.com/apikey",
      "requiredAtBoot": false,
      "generatedForDevelopment": false
    }
  ]
}
