{
  "previewPort": 3000,
  "healthPath": "/",
  "startCommand": "docker compose -f docker-compose.base44.yml up -d",
  "secrets": [
    {
      "name": "GEMINI_API_KEY",
      "description": "Google Gemini API key for the AI audit and team-assignment endpoints. Optional — the app falls back to deterministic IE heuristics when not set. Obtain from Google AI Studio (aistudio.google.com).",
      "requiredAtBoot": false
    }
  ]
}
